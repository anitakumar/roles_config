apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "auth-config.fullname" . }}-test-connection"
  labels:
    {{- include "auth-config.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "auth-config.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
